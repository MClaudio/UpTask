<!DOCTYPE html>
html(lang="es")
    head
        include partials/head
    body.login
        .contenedor-formulario 
            h1 Inicia Sesion en UpTask

            if locals.mensajes 
                .alertas 
                    each  categoria in Object.keys(locals.mensajes)
                        each error in mensajes[categoria]
                            .alerta(class=`${categoria}`)
                                p= error

            form#formulario.caja.caja-login(action="/iniciar-sesion" method="POST")
                .campo
                    label(for="email") E-mail
                    input(type="email" name="email" placeholder="Tu Email" value= email)
                .campo
                    label(for="password") Contraseña
                    input(type="password" name="password" placeholder="Tu contraseña" value= pass)
                .campo.enviar
                    input.boton(type="submit" value="Iniciar Sesion")
                .campo.acciones
                    a(href="/crear-cuenta") Crear una cuenta

                    a(href="/reestablecer") ¿Olvidaste tu contraseña?
        script(src="/dist/bundle.js")